import library.common.contract_modules.constants.files as files

# Contract
CONTRACT_FILE = "library/loan/contracts/loan.py"

CONTRACT_MODULES_ALIAS_FILE_MAP = {
    "utils": files.UTILS_FILE,
    "amortisation": files.AMORTISATION_FILE,
}
